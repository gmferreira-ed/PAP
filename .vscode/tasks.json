{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Backend",
            "type": "shell",
            "command": "npm run build",
            "problemMatcher": [],
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}/PAP-BackEnd/",
            }
        },
    ]
}